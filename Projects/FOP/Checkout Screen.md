Need to figure out the endpoint for calculating the trip name.

Clicking purchase opens the trip attribute sheet if attributes need to be captured.

Clicking on the payment button also opens the attribute sheet if needed.

Should we account for disallowing personal cards in the checkout flow? No for now

If they choose not to save the card, how does the card get saved? Do we always add it to their profile? If not, just apply it to the booking only without saving to the wallet.

