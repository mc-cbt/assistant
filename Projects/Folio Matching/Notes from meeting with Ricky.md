* llamaparse
	* Converts pdf to markdown
	* Allow LLMs to utilize
* We take the PDF, create a row in the DB, then a user manually matches to a trip
* Colter built a manual folio upload
* Temporal 
	* Currently disabled
* Dispi python AI library
* 